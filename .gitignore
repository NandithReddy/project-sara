# Virtualenv
.venv/

# Python bytecode
__pycache__/
*.pyc

# Downloaded corpora — never committed (see data/README.md for licenses)
data/raw/

# uv project lockfile. NOT our pin source -- CLAUDE.md section 5 designates
# requirements.txt. Auto-created by `uv run`; ignored to avoid two sources of
# truth that can disagree.
uv.lock

# Tool caches
.pytest_cache/
.ruff_cache/

# macOS
.DS_Store
