# Virtualenv
.venv/

# Python bytecode
__pycache__/
*.pyc

# Downloaded corpora — never committed (see data/README.md for licenses)
data/raw/

# uv project lockfile. NOT our pin source -- ENGINEERING.md section 5 designates
# requirements.txt. Auto-created by `uv run`; ignored to avoid two sources of
# truth that can disagree.
uv.lock

# Tool caches
.pytest_cache/
.ruff_cache/

# macOS
.DS_Store

# Local phase-planning notes, not part of the deliverable
PROMPT.md

# fp32 export kept beside the shipped INT8 model; 4x the size, same ranking
models/*/model_fp32.onnx

# Training-turn audio: ~1GB FLAC, reproducible from turns.jsonl via scripts/fetch_train_audio.py
data/train/audio/
